define([
    'jquery',
    'underscore',
    'd3',
    'pouch',
    'templates'
    ], function($, _, d3, Pouch, templates) {
      var PouchVision = {}

      PouchVision.start = function() {
        console.log("starting...")
      }

      return PouchVision
    })
